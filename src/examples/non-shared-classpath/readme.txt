usually a distributed actor system expects all nodes to have access to all application classes.
this example shows how an actor based service can be exposed requiring the client to only hava access
to an 'interface' package (common-interface)